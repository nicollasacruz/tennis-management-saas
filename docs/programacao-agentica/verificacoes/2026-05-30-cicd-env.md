# Verificacao: workflow e ambiente da VPS

## Comandos executados

```bash
npm --workspace apps/api run test
```

Resultado: passou.

```bash
docker compose config --services
```

Resultado: a configuracao Compose e valida e lista os servicos esperados.

```bash
ssh root@62.169.28.198 'cd /root/projects/tennis-management-saas && grep -E "^(WEB_PORT|SAAS_ROOT_DOMAIN|WEB_VIRTUAL_HOSTS|WEB_LETSENCRYPT_HOSTS|LETSENCRYPT_EMAIL|PUBLIC_API_BASE_URL|EVOLUTION_GO_VIRTUAL_HOST)=" .env && stat -c "%a %n" .env'
```

Resultado: `.env` existe, tem permissao `600` e contem as variaveis operacionais esperadas.

## Resultado observado

- `WEB_PORT=8081`
- `SAAS_ROOT_DOMAIN=tenis.esaf.run.place`
- `WEB_VIRTUAL_HOSTS=app.tenis.esaf.run.place,demo.tenis.esaf.run.place,esaf.tenis.esaf.run.place`
- `WEB_LETSENCRYPT_HOSTS=app.tenis.esaf.run.place,demo.tenis.esaf.run.place,esaf.tenis.esaf.run.place`
- `PUBLIC_API_BASE_URL=https://esaf.tenis.esaf.run.place/api`
- `EVOLUTION_GO_VIRTUAL_HOST=evolution.tenis.esaf.run.place`

## Riscos residuais

- O workflow foi revisto localmente, mas ainda nao foi executado pelo GitHub Actions depois desta alteracao.
- O reverse proxy ainda depende dos hosts fixos atuais ate a parametrizacao planeada na Fase 1.
