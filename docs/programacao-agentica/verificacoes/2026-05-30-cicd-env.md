# Verificacao: workflow e ambiente da VPS

## Comandos executados

```bash
npm --workspace apps/api run test
```

Resultado: passou.

```bash
docker compose config --services
```

Resultado: a configuracao Compose e valida e lista os servicos esperados.

```bash
ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci-cd.yml"); puts "yaml ok"'
```

Resultado: workflow parseado como YAML valido.

```bash
ssh root@62.169.28.198 'cd /root/projects/tennis-management-saas && grep -E "^(WEB_PORT|SAAS_ROOT_DOMAIN|WEB_VIRTUAL_HOSTS|WEB_LETSENCRYPT_HOSTS|LETSENCRYPT_EMAIL|PUBLIC_API_BASE_URL|EVOLUTION_API_VIRTUAL_HOST|EVOLUTION_GO_VIRTUAL_HOST)=" .env && stat -c "%a %n" .env'
```

Resultado: `.env` existe, tem permissao `600` e contem as variaveis operacionais esperadas.

## Resultado observado

- `WEB_PORT=8081`
- `SAAS_ROOT_DOMAIN=tenis.clubtenispro.com`
- `WEB_VIRTUAL_HOSTS=app.tenis.clubtenispro.com,demo.tenis.clubtenispro.com,demo.tenis.clubtenispro.com`
- `WEB_LETSENCRYPT_HOSTS=app.tenis.clubtenispro.com,demo.tenis.clubtenispro.com,demo.tenis.clubtenispro.com`
- `PUBLIC_API_BASE_URL=https://demo.tenis.clubtenispro.com/api`
- `EVOLUTION_API_VIRTUAL_HOST=tenisevolution.clubtenispro.com`

## Riscos residuais

- O workflow foi revisto localmente, mas ainda precisa de nova execucao no GitHub Actions depois desta alteracao.
- O secret `DEPLOY_SSH_KEY` continua obrigatorio no repositorio.
- O reverse proxy ainda depende dos hosts fixos atuais ate a parametrizacao planeada na Fase 1.
